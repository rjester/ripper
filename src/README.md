Developer notes for `src`:

- Preferred invocation for local development:

```bash
python cli.py greet --name <name> --verbose
```

- The module exposes a Typer app and can be installed as a console script named `ripper` (see project `pyproject.toml`).

Keep this file for short developer-facing tips.
